# JavaScript

Code standards and patterns for JavaScript in this Defra CDP application.

## Scope

Applies to all `.js` files in `src/`.
- Use vanilla JavaScript with JSDoc for type annotations.
- Use ES Modules with named exports.
- Use absolute imports with `~` alias for internal project files.
- Use Hapi.js exclusively as the server framework.
- Use `fetch` for all API calls.


